# Database Configuration (REQUIRED)
# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database_name
# Example for local PostgreSQL: postgresql://postgres:password@localhost:5432/supportdesk
DATABASE_URL=postgresql://postgres:password@localhost:5432/supportdesk

# Session Security (REQUIRED for production)
# Generate a random secret key for session encryption
SESSION_SECRET=your-secret-key-here-change-this-in-production

# OpenAI API Key (REQUIRED for AI features)
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-placeholder-replace-with-your-actual-key

# IMAP Configuration (for email ingestion)
# Leave empty if not using email auto-fetch
IMAP_HOST=
IMAP_PORT=993
IMAP_USERNAME=
IMAP_PASSWORD=

# SMTP Configuration (for sending emails)
# Leave empty if not using email sending
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=

# Slack Webhook (optional)
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL
